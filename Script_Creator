local HttpService = game:GetService("HttpService")
local WEBHOOK_URL = "https://discord.com/api/webhooks/1378878453293650041/4ENx3GlPurpgQxPckeSYplZUurHREcdSejG-pIpYi8lmXuFKr7xxZ7I78IncKX8yX4Ca"

-- ✅ Smart request handler
local requestFunc = (syn and syn.request) or (http and http.request) or request or http_request

-- ✅ Skip noisy remotes
local skipRemotes = {
    ["Click"] = true,
    ["Send Position"] = true,
    ["SendLikelySpeakingUsers"] = true,
    ["__BLUNDER"] = true,
}

-- ✅ Webhook sender
local function sendWebhook(msg)
    if not requestFunc or not WEBHOOK_URL then return end
    local success, err = pcall(function()
        requestFunc({
            Url = WEBHOOK_URL,
            Method = "POST",
            Headers = { ["Content-Type"] = "application/json" },
            Body = HttpService:JSONEncode({ content = msg })
        })
    end)
    if not success then
        warn("[Webhook Error]", err)
    end
end

-- ✅ Error reporting
game:GetService("LogService").MessageOut:Connect(function(msg, msgType)
    if msgType == Enum.MessageType.MessageError then
        sendWebhook("❌ **Error:** " .. msg)
    elseif msgType == Enum.MessageType.MessageWarning then
        sendWebhook("⚠️ **Warning:** " .. msg)
    end
end)

-- ✅ Remote tracker
local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local args = { ... }

    if not checkcaller() and (method == "FireServer" or method == "InvokeServer") then
        local remoteName = tostring(self):match("[^%.]+$") or tostring(self)

        if skipRemotes[remoteName] then
            return oldNamecall(self, ...)
        end

        local success, msg = pcall(function()
            local msg = "📡 **Remote Call Detected**\n"
            msg = msg .. "**Remote:** `" .. tostring(self) .. "`\n"
            msg = msg .. "**Method:** `" .. method .. "`"

            for i, v in ipairs(args) do
                local str
                local ok, result = pcall(function()
                    return HttpService:JSONEncode(v)
                end)
                str = ok and result or tostring(v)
                msg = msg .. string.format("\n`Arg %d`: ```%s```", i, str)
            end

            return msg
        end)

        if success and msg then
            sendWebhook(msg)
        else
            sendWebhook("❌ **Error formatting remote call:** " .. tostring(msg))
        end
    end

    return oldNamecall(self, ...)
end))

-- ✅ Confirmation
sendWebhook("✅ Webhook remote logger loaded and stable.")
print("✅ Webhook logger running.")
